# tabstride-protocol

Rust types and JSON Schemas for the tabstride wire protocol (CLI ↔ daemon ↔ extension).

Generated JSON Schemas live in `schema/`.

```bash
cargo run -p tabstride-protocol --bin dump-schema --locked
```
