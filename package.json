{
  "name": "tabstride",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "cli:build": "cargo run -p tabstride-protocol --bin dump-schema --locked && pnpm -r build",
    "ext:build": "pnpm --filter @tabstride/extension build",
    "ext:build:zip": "pnpm --filter @tabstride/extension zip",
    "ext:dev": "pnpm --filter @tabstride/extension dev",
    "ext:test": "pnpm --filter @tabstride/extension test",
    "e2e:chrome": "node scripts/actionability-chrome-e2e.mjs --prepare --mode e2e",
    "perf:chrome": "node scripts/actionability-chrome-e2e.mjs --prepare --mode benchmark --report artifacts/actionability-performance.json",
    "perf:task": "node scripts/actionability-chrome-e2e.mjs --prepare --mode task-baseline --task-report artifacts/single-task-performance.json",
    "test:chrome": "node scripts/actionability-chrome-e2e.mjs --prepare --mode all --report artifacts/actionability-performance.json",
    "lint": "biome check . && stylelint \"**/*.css\"",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "stylelint": "^16.0.0",
    "stylelint-config-standard": "^37.0.0"
  }
}
